git checkout  -b develop
git commit -a "add useful links to REAME.md"
git checkout -b release/0.1.0
git checkout develop
git merge release/0.1.0
git branch -D release/0.1.0
git checkout master
git merge develop
git tag -a v0.1.0 -m "first release"
